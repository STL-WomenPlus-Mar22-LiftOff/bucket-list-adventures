<a asp-controller="Search" asp-action="ClimateData" 
asp-route-latitude="@ViewBag.latitude" asp-route-longitude="@ViewBag.longitude">
Temperature data by month</a>

<ul>
    @foreach (var activity in ViewBag.activitiesObject)
    {
            <li>
                <a href="@Url.Action("Details","Home")?activity=@activity.name">  @activity.name </a>
            </li>
    }
</ul>